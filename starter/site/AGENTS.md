# Resumilio project

Use the bundled `.resumilio/skills/resumilio-authoring/SKILL.md` whenever work interviews for, imports, maps, validates, or publishes this career profile.

- Keep private source files and notes outside the public project.
- Never invent career facts or public availability.
- Use job-market language in public UI.
- Run `npx resumilio validate resumilio.json` and `npm run build` before delivery.
- Local preview is allowed; external publication requires the user's authorization.
