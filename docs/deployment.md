# Deployment

Run `npm run build` in a generated project and publish its `dist/` directory to any static host.

For Cloudflare Pages, use `npm run build` as the build command and `dist` as the output directory. Configure the custom domain in Cloudflare after verifying the preview deployment. No provider-specific runtime is required.
